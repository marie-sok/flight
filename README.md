# flight

Archive placeholder.

The repository did not contain application source code; it only contained IDE metadata. The cleanup branch removes that metadata and leaves a clean base for either restoring the original project or archiving the repository intentionally.
